# Cinemap Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a static React movie discovery site (Cineby-inspired, movies only) in a `cinemap/` folder inside the website_automation repo.

**Architecture:** Vite + React SPA with no router — a single Home page assembling a HeroBanner and multiple MovieRow carousels, with a MovieModal overlay for details. All data is hardcoded TMDB-shaped JSON; poster/backdrop images are served from the public TMDB image CDN (no API key needed).

**Tech Stack:** Vite 5, React 18, plain CSS (no Tailwind, no CSS-in-JS libraries)

## Global Constraints

- Folder: `cinemap/` at repo root (`/Users/waqiur/Personal/website_automation/cinemap/`)
- No TypeScript — plain `.jsx` / `.js`
- No CSS frameworks — plain CSS only
- No routing library — single page
- Image base URL: `https://image.tmdb.org/t/p/w500` for posters, `https://image.tmdb.org/t/p/original` for backdrops
- Background color: `#0d0d0d`, accent: `#e50914`, primary text: `#ffffff`, secondary text: `#aaaaaa`
- No external state management — plain React useState/props

---

### Task 1: Scaffold Vite + React project

**Files:**
- Create: `cinemap/package.json`
- Create: `cinemap/vite.config.js`
- Create: `cinemap/index.html`
- Create: `cinemap/src/main.jsx`
- Create: `cinemap/src/App.jsx`
- Create: `cinemap/src/index.css`

**Interfaces:**
- Produces: running dev server at `http://localhost:5173` rendering `<App />`

- [ ] **Step 1: Create the cinemap folder and package.json**

```bash
mkdir -p /Users/waqiur/Personal/website_automation/cinemap/src
```

Create `cinemap/package.json`:
```json
{
  "name": "cinemap",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.2"
  }
}
```

- [ ] **Step 2: Create vite.config.js**

Create `cinemap/vite.config.js`:
```js
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
})
```

- [ ] **Step 3: Create index.html**

Create `cinemap/index.html`:
```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Cinemap</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.jsx"></script>
  </body>
</html>
```

- [ ] **Step 4: Create src/main.jsx**

Create `cinemap/src/main.jsx`:
```jsx
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './index.css'
import App from './App.jsx'

createRoot(document.getElementById('root')).render(
  <StrictMode>
    <App />
  </StrictMode>
)
```

- [ ] **Step 5: Create src/App.jsx**

Create `cinemap/src/App.jsx`:
```jsx
export default function App() {
  return <div>Cinemap loading...</div>
}
```

- [ ] **Step 6: Create src/index.css**

Create `cinemap/src/index.css`:
```css
*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

:root {
  --bg: #0d0d0d;
  --accent: #e50914;
  --text-primary: #ffffff;
  --text-secondary: #aaaaaa;
  --card-bg: #1a1a1a;
}

body {
  background-color: var(--bg);
  color: var(--text-primary);
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  min-height: 100vh;
}

img {
  display: block;
  max-width: 100%;
}

button {
  cursor: pointer;
  border: none;
  background: none;
  font-family: inherit;
}
```

- [ ] **Step 7: Install dependencies and verify dev server starts**

```bash
cd /Users/waqiur/Personal/website_automation/cinemap && npm install
```

```bash
cd /Users/waqiur/Personal/website_automation/cinemap && npm run dev &
sleep 3 && curl -s http://localhost:5173 | head -20
```

Expected: HTML response containing `<div id="root">` — confirms Vite is serving.

- [ ] **Step 8: Commit**

```bash
cd /Users/waqiur/Personal/website_automation && git add cinemap/ && git commit -m "feat: scaffold cinemap vite+react project"
```

---

### Task 2: Movie data (TMDB-shaped mock)

**Files:**
- Create: `cinemap/src/data/movies.js`

**Interfaces:**
- Produces: `export const movies` — array of 30 movie objects, each with: `id`, `title`, `overview`, `poster_path`, `backdrop_path`, `release_date`, `vote_average`, `vote_count`, `genre_ids`, `runtime`, `tagline`
- Produces: `export const genres` — `{ 28: 'Action', 18: 'Drama', 878: 'Sci-Fi', 35: 'Comedy', 53: 'Thriller', 10749: 'Romance', 27: 'Horror', 80: 'Crime', 12: 'Adventure', 16: 'Animation' }`
- Produces: `export const FEATURED_IDS` — array of 3 movie ids to feature in HeroBanner

- [ ] **Step 1: Create cinemap/src/data/movies.js**

Create `cinemap/src/data/movies.js`:
```js
export const genres = {
  28: 'Action',
  18: 'Drama',
  878: 'Sci-Fi',
  35: 'Comedy',
  53: 'Thriller',
  10749: 'Romance',
  27: 'Horror',
  80: 'Crime',
  12: 'Adventure',
  16: 'Animation',
}

export const FEATURED_IDS = [550, 27205, 157336]

export const movies = [
  {
    id: 550,
    title: 'Fight Club',
    tagline: 'Mischief. Mayhem. Soap.',
    overview: 'A ticking-time-bomb insomniac and a slippery soap salesman channel primal male aggression into a shocking new form of therapy. Their concept catches on, with underground fight clubs forming in every town, until an eccentric gets in the way and ignites an out-of-control spiral toward oblivion.',
    poster_path: '/pB8BM7pdSp6B6Ih7QZ4DrQ3PmJK.jpg',
    backdrop_path: '/hZkgoQYus5vegHoetLkCJzVPrWl.jpg',
    release_date: '1999-10-15',
    vote_average: 8.4,
    vote_count: 26280,
    genre_ids: [18, 53],
    runtime: 139,
  },
  {
    id: 238,
    title: 'The Godfather',
    tagline: 'An offer you can\'t refuse.',
    overview: 'Spanning the years 1945 to 1955, a chronicle of the fictional Italian-American Corleone crime family. When organized crime family patriarch, Vito Corleone barely survives an attempt on his life, his youngest son, Michael steps in to take care of the would-be killers, launching a campaign of bloody revenge.',
    poster_path: '/3bhkrj58Vtu7enYsLegHQDKJ7cA.jpg',
    backdrop_path: '/tmU7GeKVybMWFButWEGl2M4GeiP.jpg',
    release_date: '1972-03-14',
    vote_average: 8.7,
    vote_count: 18000,
    genre_ids: [18, 80],
    runtime: 175,
  },
  {
    id: 278,
    title: 'The Shawshank Redemption',
    tagline: 'Fear can hold you prisoner. Hope can set you free.',
    overview: 'Framed in the 1940s for the double murder of his wife and her lover, upstanding banker Andy Dufresne begins a new life at the Shawshank prison, where he puts his accounting skills to work for an amoral warden. During his long stretch in prison, Dufresne comes to be admired by the other inmates.',
    poster_path: '/lyQBXzOQSuE59IsHyhrp0qIiPAz.jpg',
    backdrop_path: '/kXfqcdQKsToO0OUXHcrrNCHDBzO.jpg',
    release_date: '1994-09-23',
    vote_average: 8.7,
    vote_count: 24000,
    genre_ids: [18, 80],
    runtime: 142,
  },
  {
    id: 27205,
    title: 'Inception',
    tagline: 'Your mind is the scene of the crime.',
    overview: 'Cobb, a skilled thief who commits corporate espionage by infiltrating the subconscious of his targets is offered a chance to regain his old life as payment for a task considered to be impossible: "inception", the implantation of another person\'s idea into a target\'s subconscious.',
    poster_path: '/oYuLEt3zVCKq57qu2F8dT7NIa6f.jpg',
    backdrop_path: '/s3TBrRGB1iav7gFOCNx3H31MoES.jpg',
    release_date: '2010-07-15',
    vote_average: 8.4,
    vote_count: 34000,
    genre_ids: [28, 878, 12],
    runtime: 148,
  },
  {
    id: 157336,
    title: 'Interstellar',
    tagline: 'Mankind was born on Earth. It was never meant to die here.',
    overview: 'The adventures of a group of explorers who make use of a newly discovered wormhole to surpass the limitations on human space travel and conquer the vast distances involved in an interstellar voyage.',
    poster_path: '/gEU2QniE6E77NI6lCU6MxlNBvIx.jpg',
    backdrop_path: '/xJHokMbljvjADYdit5fK5VQsXEG.jpg',
    release_date: '2014-11-05',
    vote_average: 8.4,
    vote_count: 32000,
    genre_ids: [12, 878, 18],
    runtime: 169,
  },
  {
    id: 155,
    title: 'The Dark Knight',
    tagline: 'Why so serious?',
    overview: 'Batman raises the stakes in his war on crime. With the help of Lt. Jim Gordon and District Attorney Harvey Dent, Batman sets out to dismantle the remaining criminal organizations that plague the streets. The partnership proves to be effective, but they soon find themselves prey to a reign of chaos unleashed by a rising criminal mastermind known to the terrified citizens of Gotham as the Joker.',
    poster_path: '/qJ2tW6WMUDux911r6m7haRef0WH.jpg',
    backdrop_path: '/hkBaDkMWbLaf8B1lsWsNprs3oeO.jpg',
    release_date: '2008-07-14',
    vote_average: 8.5,
    vote_count: 30000,
    genre_ids: [28, 80, 18, 53],
    runtime: 152,
  },
  {
    id: 769,
    title: 'GoodFellas',
    tagline: 'Three Decades of Life in the Mafia.',
    overview: 'The true story of Henry Hill, a half-Irish, half-Sicilian Brooklyn kid who is adopted by the Mafia. He lives the high life until his partners in crime turn against him.',
    poster_path: '/aKuFiU82s5ISJpGZp7YkIr3kCUd.jpg',
    backdrop_path: '/sw7mordbZxgITU877yTpZCud90M.jpg',
    release_date: '1990-09-12',
    vote_average: 8.5,
    vote_count: 11000,
    genre_ids: [18, 80],
    runtime: 146,
  },
  {
    id: 680,
    title: 'Pulp Fiction',
    tagline: 'Just because you are a character doesn\'t mean you have character.',
    overview: 'A burger-loving hit man, his philosophical partner, a drug-addled gangster\'s moll and a washed-up boxer converge in this sprawling, comedic crime caper. Their adventures unfurl in three stories that ingeniously trip back and forth in time.',
    poster_path: '/d5iIlFn5s0ImszYzBPb8JPIfbXD.jpg',
    backdrop_path: '/4cDFJr4HnXN5AdPw4AKrmLlMWdO.jpg',
    release_date: '1994-09-10',
    vote_average: 8.5,
    vote_count: 25000,
    genre_ids: [53, 80],
    runtime: 154,
  },
  {
    id: 13,
    title: 'Forrest Gump',
    tagline: 'Life is like a box of chocolates...you never know what you\'re gonna get.',
    overview: 'A man with a low IQ has accomplished great things in his life and been present during significant historic events—in each case, far exceeding what anyone imagined he could do. But despite all the things he has attained, his one true love eludes him.',
    poster_path: '/arw2vcBveWOVZr6pxd9XTd1TdQa.jpg',
    backdrop_path: '/qdIMHd4sEfJSckfVJfKQvisL02a.jpg',
    release_date: '1994-06-23',
    vote_average: 8.5,
    vote_count: 24000,
    genre_ids: [35, 18, 10749],
    runtime: 142,
  },
  {
    id: 11,
    title: 'Star Wars',
    tagline: 'A long time ago in a galaxy far, far away....',
    overview: 'Princess Leia is captured and held hostage by the evil Imperial forces in their effort to take over the galactic Empire. Venturesome Luke Skywalker and dashing captain Han Solo team together with the loveable robot duo R2-D2 and C-3PO to rescue the beautiful princess and restore peace and justice in the Empire.',
    poster_path: '/6FfCtAuVAW8XJjZ7eWeLibRLWTw.jpg',
    backdrop_path: '/4iJfYYoQzZcONB9hNzg0J0wWPwS.jpg',
    release_date: '1977-05-25',
    vote_average: 8.2,
    vote_count: 18000,
    genre_ids: [28, 12, 878],
    runtime: 121,
  },
  {
    id: 389,
    title: 'Twelve Angry Men',
    tagline: 'Life is in their hands -- Death is on their minds!',
    overview: 'The defense and the prosecution have rested and the jury is filing into the jury room to decide if a young man is guilty or innocent of murdering his father. What begins as an open-and-shut case of murder soon becomes a detective story that presents a succession of clues creating doubt, and a mini-drama of each juror\'s prejudices and preconceptions about the trial, the accused, and each other.',
    poster_path: '/qqHQsStV6exghCM7zbObuYBiYxw.jpg',
    backdrop_path: '/qqHQsStV6exghCM7zbObuYBiYxw.jpg',
    release_date: '1957-04-10',
    vote_average: 8.9,
    vote_count: 8000,
    genre_ids: [18],
    runtime: 96,
  },
  {
    id: 424,
    title: 'Schindler\'s List',
    tagline: 'Whoever saves one life, saves the world entire.',
    overview: 'The true story of how businessman Oskar Schindler saved over a thousand Jewish lives from the Nazis while they worked as slaves in his factory during World War II.',
    poster_path: '/sF1U4EUQS8YHUYjNl3pMGNIQyr0.jpg',
    backdrop_path: '/loRmRzQXZeqG78TqZEJtGouqNPh.jpg',
    release_date: '1993-11-30',
    vote_average: 8.9,
    vote_count: 14000,
    genre_ids: [18, 12],
    runtime: 195,
  },
  {
    id: 637,
    title: 'Life is Beautiful',
    tagline: 'An unforgettable fable that proves love, family and imagination conquer all.',
    overview: 'A touching story of an Italian book seller of Jewish ancestry who lives in his own little fairy tale. His creative and happy life would come to an abrupt halt when his entire family gets deported to a concentration camp during World War II.',
    poster_path: '/74hLDKjAuDdreflerJC9V4Mq7BL.jpg',
    backdrop_path: '/79SXgzPNFi6t9MaVWWvVt7c7sFw.jpg',
    release_date: '1997-12-20',
    vote_average: 8.5,
    vote_count: 14000,
    genre_ids: [35, 18, 10749],
    runtime: 116,
  },
  {
    id: 19404,
    title: 'Dilwale Dulhania Le Jayenge',
    tagline: 'Come Fall in Love',
    overview: 'Raj is a rich, carefree, happy-go-lucky second generation NRI. Simran is the daughter of Chaudhary Baldev Singh, who in spite of being settled in London, is very strict about adherence to Indian values. Raj and Simran meet in Europe and fall in love, but Simran gets engaged to Kuljeet.',
    poster_path: '/2CAL2433ZeIihfX1Hb2139CX0pW.jpg',
    backdrop_path: '/90ez6ArvpO7zA82Zx0enRIGeVCh.jpg',
    release_date: '1995-10-20',
    vote_average: 8.6,
    vote_count: 4000,
    genre_ids: [18, 10749],
    runtime: 189,
  },
  {
    id: 101,
    title: 'Léon: The Professional',
    tagline: 'The first rule of being a professional: never get involved.',
    overview: 'Mathilda, a 12-year-old girl, is reluctantly taken in by Léon, a professional cleaner, after her family is murdered. Léon and Mathilda form an unusual relationship, as she becomes his protégée and learns the assassin\'s trade.',
    poster_path: '/yI6X2cCM5YPJtxMhUd3dPGqDAhw.jpg',
    backdrop_path: '/mSweXFGt1rXrHPFApMJjMfgpUiQ.jpg',
    release_date: '1994-09-14',
    vote_average: 8.3,
    vote_count: 13000,
    genre_ids: [28, 53, 18],
    runtime: 110,
  },
  {
    id: 496243,
    title: 'Parasite',
    tagline: 'Act like you own the place.',
    overview: 'All unemployed, Ki-taek\'s family takes peculiar interest in the wealthy and glamorous Parks for their livelihood until they get entangled in an unexpected incident.',
    poster_path: '/7IiTTgloJzvGI1TAYymCfbfl3vT.jpg',
    backdrop_path: '/TU9NIjwzjoKPwQHoVclLDj6UYVF.jpg',
    release_date: '2019-05-30',
    vote_average: 8.5,
    vote_count: 16000,
    genre_ids: [35, 53, 18],
    runtime: 132,
  },
  {
    id: 372058,
    title: 'Your Name.',
    tagline: 'What\'s your name?',
    overview: 'High schoolers Mitsuha and Taki are complete strangers living separate lives. But one night, they suddenly switch places. Mitsuha wakes up in Taki\'s body, and he in hers. This bizarre occurrence continues to happen randomly, and the two must adjust their lives around each other.',
    poster_path: '/q719jXXEzOoYaps6babgKnONONX.jpg',
    backdrop_path: '/mMtUybQ6hL24FXo0F3Z4j2KG7kZ.jpg',
    release_date: '2016-08-26',
    vote_average: 8.4,
    vote_count: 9000,
    genre_ids: [16, 10749, 18, 878],
    runtime: 106,
  },
  {
    id: 324857,
    title: 'Spider-Man: Into the Spider-Verse',
    tagline: 'More than one wears the mask.',
    overview: 'Miles Morales is juggling his life between being a high school student and being Spider-Man. However, when Wilson "Kingpin" Fisk uses a super collider, another Spider-Man from another dimension, Peter Parker, accidentally winds up in Miles\' dimension.',
    poster_path: '/iiZZdoQBEYBv6id8su7ImL0oCbD.jpg',
    backdrop_path: '/7du3Z8bFfKLqwRbJrPxONNa05mb.jpg',
    release_date: '2018-12-14',
    vote_average: 8.4,
    vote_count: 12000,
    genre_ids: [28, 16, 12],
    runtime: 117,
  },
  {
    id: 569094,
    title: 'Spider-Man: Across the Spider-Verse',
    tagline: 'Every hero can\'t save everyone.',
    overview: 'After reuniting with Gwen Stacy, Brooklyn\'s full-time, friendly neighborhood Spider-Man is catapulted across the Multiverse, where he encounters a team of Spider-People charged with protecting its very existence.',
    poster_path: '/8Vt6mWEReuy4Of61Lnj5Xj704m8.jpg',
    backdrop_path: '/1X7vow16X7CnCoRQHXi4ygaFRjw.jpg',
    release_date: '2023-05-31',
    vote_average: 8.7,
    vote_count: 8000,
    genre_ids: [28, 16, 12, 878],
    runtime: 140,
  },
  {
    id: 120,
    title: 'The Lord of the Rings: The Fellowship of the Ring',
    tagline: 'One ring to rule them all.',
    overview: 'Young hobbit Frodo Baggins, after inheriting a mysterious ring from his uncle Bilbo, must leave his home in order to keep it from the grasp of its evil creator. Along the way, a fellowship is formed to protect the ring-bearer and a crew of allies joins Frodo on his epic quest to destroy the one Ring in the fires of Mount Doom.',
    poster_path: '/6oom5QYQ2yQTMJIbnvbkBL9cHo6.jpg',
    backdrop_path: '/pIgHGHEoOhgMPmS1wFnXZn4FVBP.jpg',
    release_date: '2001-12-19',
    vote_average: 8.4,
    vote_count: 22000,
    genre_ids: [12, 878],
    runtime: 178,
  },
  {
    id: 122,
    title: 'The Lord of the Rings: The Return of the King',
    tagline: 'The eye of the enemy is moving.',
    overview: 'Aragorn is revealed as the heir to the ancient kings as he and his allies take a last stand against Sauron\'s evil before Frodo and Sam reach Mount Doom.',
    poster_path: '/rCzpDGLbOoPwLjy3OAm5NUPOTrC.jpg',
    backdrop_path: '/lXhgCODAbBXL5buk9yEmTpOoOgR.jpg',
    release_date: '2003-12-01',
    vote_average: 8.5,
    vote_count: 21000,
    genre_ids: [12, 878, 28],
    runtime: 201,
  },
  {
    id: 598,
    title: 'City of God',
    tagline: 'If you run you\'re dead... if you stay, you\'re dead again. Period.',
    overview: 'Cidade de Deus is a housing project in the West Zone of Rio de Janeiro. In the 1960s it was built to re-house the inhabitants from the favelas. By the time the film begins in the 1970s it has become one of the most dangerous places in Rio.',
    poster_path: '/k7eYdWvhYQyRQoU2TB2A2Xu2TfD.jpg',
    backdrop_path: '/9cFBaguKPXeNkyLXdPeqOHONTny.jpg',
    release_date: '2002-08-30',
    vote_average: 8.6,
    vote_count: 8000,
    genre_ids: [80, 18],
    runtime: 130,
  },
  {
    id: 1422,
    title: 'The Departed',
    tagline: 'Lies. Betrayal. Sacrifice. How far will you go?',
    overview: 'To take down South Boston\'s Irish Mafia, the police send in one of their own to infiltrate the underworld, not realizing the syndicate has done likewise. While an undercover cop curries favor with the mob kingpin, a career criminal rises through the police ranks. But both sides soon discover there\'s a mole among them.',
    poster_path: '/nT97ifVT2J1yMQmeq20Qblg61T.jpg',
    backdrop_path: '/tWOBO3FXz1GIxnaFGc4LoNwQCzF.jpg',
    release_date: '2006-10-05',
    vote_average: 8.2,
    vote_count: 14000,
    genre_ids: [18, 53, 80],
    runtime: 151,
  },
  {
    id: 761053,
    title: 'Gabriel\'s Inferno',
    tagline: 'Forbidden Love',
    overview: 'An enigmatic professor falls deeply in love with his graduate student. Unbeknownst to him, she is the little girl who stole his heart many years earlier.',
    poster_path: '/oyG9TL9OcBmGbRCrIFcFpCOFmqH.jpg',
    backdrop_path: '/3GrRgt6CiLIUXlights5Ell2bliL.jpg',
    release_date: '2020-05-29',
    vote_average: 8.5,
    vote_count: 3000,
    genre_ids: [10749, 18],
    runtime: 97,
  },
  {
    id: 346,
    title: 'Seven',
    tagline: 'Seven deadly sins. Seven ways to die.',
    overview: 'Two homicide detectives are on a desperate hunt for a serial killer whose crimes are based on the "seven deadly sins" in this dark and haunting film that takes viewers from the tortured remains of the killed to the final wrenching death.',
    poster_path: '/6yoghtyTpznpBik8EngEmJskVnS.jpg',
    backdrop_path: '/uu0OwluCBdBFswXbWl0zmXpXQPP.jpg',
    release_date: '1995-09-22',
    vote_average: 8.4,
    vote_count: 16000,
    genre_ids: [80, 18, 53, 27],
    runtime: 127,
  },
  {
    id: 807,
    title: 'Se7en',
    tagline: 'Wanting people to listen, you can\'t just tap them on the shoulder anymore.',
    overview: 'Interchangeable with Seven above — placeholder for data variety.',
    poster_path: '/6yoghtyTpznpBik8EngEmJskVnS.jpg',
    backdrop_path: '/uu0OwluCBdBFswXbWl0zmXpXQPP.jpg',
    release_date: '1995-09-22',
    vote_average: 8.1,
    vote_count: 9000,
    genre_ids: [27, 53],
    runtime: 95,
  },
  {
    id: 475557,
    title: 'Joker',
    tagline: 'Put on a happy face.',
    overview: 'During the 1980s, a failed stand-up comedian is driven insane and turns to a life of crime and chaos in Gotham City while becoming an infamous psychopathic crime figure.',
    poster_path: '/udDclJoHjfjb8Ekgsd4FDteOkCU.jpg',
    backdrop_path: '/f5F4cRhQdUbyVbB5lTNCwUzD6BP.jpg',
    release_date: '2019-10-02',
    vote_average: 8.2,
    vote_count: 23000,
    genre_ids: [80, 18, 53],
    runtime: 122,
  },
  {
    id: 14160,
    title: 'Up',
    tagline: 'Adventure is out there!',
    overview: '78-year-old Carl Fredricksen travels to Paradise Falls in his house equipped with balloons, inadvertently taking a young stowaway.',
    poster_path: '/96mOHDSGcvGI0ECTzEkATlsIPKt.jpg',
    backdrop_path: '/ljsZTbVsrQSqZgWeep2B1QiDKuh.jpg',
    release_date: '2009-05-29',
    vote_average: 8.3,
    vote_count: 20000,
    genre_ids: [12, 16, 35],
    runtime: 96,
  },
  {
    id: 862,
    title: 'Toy Story',
    tagline: 'Hang on for the comedy that goes to infinity and beyond!',
    overview: 'Led by Woody, Andy\'s toys live happily in his room until Andy\'s birthday brings Buzz Lightyear onto the scene. Afraid of losing his place in Andy\'s heart, Woody plots against Buzz. But when circumstances separate Buzz and Woody from their owner, the duo eventually learns to put aside their differences.',
    poster_path: '/uXDfjJbdP4ijW5hWSBrPl9KaI/9o.jpg',
    backdrop_path: '/dji4Fm0gCDVb9DQQMRvAI8YNnTz.jpg',
    release_date: '1995-10-30',
    vote_average: 8.0,
    vote_count: 16000,
    genre_ids: [16, 12, 35],
    runtime: 81,
  },
  {
    id: 274,
    title: 'The Silence of the Lambs',
    tagline: 'To enter the mind of a killer she must challenge the mind of a madman.',
    overview: 'Clarice Starling is a top student at the FBI\'s training academy. Jack Crawford wants Clarice to interview Dr. Hannibal Lecter, a brilliant psychiatrist who is also a violent psychopath, serving life behind bars for various acts of murder and cannibalism.',
    poster_path: '/rplLJ2hPcOQmkFhTqUte0MkEaO2.jpg',
    backdrop_path: '/mfwq2nMBzArzQ7Y9RKE8SKeeTkg.jpg',
    release_date: '1991-02-14',
    vote_average: 8.3,
    vote_count: 14000,
    genre_ids: [80, 18, 27, 53],
    runtime: 118,
  },
]
```

- [ ] **Step 2: Verify data file is valid JS**

```bash
cd /Users/waqiur/Personal/website_automation/cinemap && node -e "import('./src/data/movies.js').then(m => console.log('movies count:', m.movies.length, '| featured:', m.FEATURED_IDS))"
```

Expected output: `movies count: 30 | featured: [ 550, 27205, 157336 ]`

- [ ] **Step 3: Commit**

```bash
cd /Users/waqiur/Personal/website_automation && git add cinemap/src/data/ && git commit -m "feat: add TMDB-shaped mock movie data"
```

---

### Task 3: Navbar component

**Files:**
- Create: `cinemap/src/components/Navbar.jsx`
- Create: `cinemap/src/components/Navbar.css`

**Interfaces:**
- Produces: `export default function Navbar()` — takes no props

- [ ] **Step 1: Create Navbar.css**

Create `cinemap/src/components/Navbar.css`:
```css
.navbar {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 100;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 48px;
  height: 64px;
  background: linear-gradient(to bottom, rgba(0,0,0,0.9) 0%, transparent 100%);
  transition: background 0.3s;
}

.navbar.scrolled {
  background: #0d0d0d;
}

.navbar-logo {
  display: flex;
  align-items: center;
  gap: 10px;
  text-decoration: none;
}

.navbar-logo-icon {
  width: 32px;
  height: 32px;
  background: var(--accent);
  border-radius: 6px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 18px;
  font-weight: 900;
  color: white;
}

.navbar-logo-text {
  font-size: 22px;
  font-weight: 800;
  color: var(--text-primary);
  letter-spacing: -0.5px;
}

.navbar-right {
  display: flex;
  align-items: center;
  gap: 24px;
}

.navbar-link {
  color: var(--text-secondary);
  font-size: 14px;
  font-weight: 500;
  text-decoration: none;
  transition: color 0.2s;
}

.navbar-link:hover {
  color: var(--text-primary);
}

.navbar-search-btn {
  color: var(--text-primary);
  font-size: 20px;
  padding: 4px;
  opacity: 0.8;
  transition: opacity 0.2s;
}

.navbar-search-btn:hover {
  opacity: 1;
}
```

- [ ] **Step 2: Create Navbar.jsx**

Create `cinemap/src/components/Navbar.jsx`:
```jsx
import { useState, useEffect } from 'react'
import './Navbar.css'

export default function Navbar() {
  const [scrolled, setScrolled] = useState(false)

  useEffect(() => {
    const onScroll = () => setScrolled(window.scrollY > 20)
    window.addEventListener('scroll', onScroll)
    return () => window.removeEventListener('scroll', onScroll)
  }, [])

  return (
    <nav className={`navbar${scrolled ? ' scrolled' : ''}`}>
      <a href="/" className="navbar-logo">
        <div className="navbar-logo-icon">C</div>
        <span className="navbar-logo-text">Cinemap</span>
      </a>
      <div className="navbar-right">
        <a href="#" className="navbar-link">Home</a>
        <a href="#" className="navbar-link">Browse</a>
        <button className="navbar-search-btn" aria-label="Search">⌕</button>
      </div>
    </nav>
  )
}
```

- [ ] **Step 3: Wire into App.jsx and verify visually**

Replace `cinemap/src/App.jsx`:
```jsx
import Navbar from './components/Navbar.jsx'

export default function App() {
  return (
    <div>
      <Navbar />
      <div style={{ paddingTop: '64px', color: 'white', padding: '100px 48px' }}>
        Cinemap — components loading
      </div>
    </div>
  )
}
```

Open `http://localhost:5173` — confirm: dark navbar with "C" logo, "Cinemap" text, Home/Browse links, search button. Scroll down — navbar should darken.

- [ ] **Step 4: Commit**

```bash
cd /Users/waqiur/Personal/website_automation && git add cinemap/src/components/Navbar.jsx cinemap/src/components/Navbar.css cinemap/src/App.jsx && git commit -m "feat: add Navbar component"
```

---

### Task 4: MovieCard component

**Files:**
- Create: `cinemap/src/components/MovieCard.jsx`
- Create: `cinemap/src/components/MovieCard.css`

**Interfaces:**
- Consumes: `movie` object (id, title, poster_path, vote_average, release_date, genre_ids), `genres` map, `onSelect` callback `(movie) => void`
- Produces: `export default function MovieCard({ movie, genres, onSelect })`

- [ ] **Step 1: Create MovieCard.css**

Create `cinemap/src/components/MovieCard.css`:
```css
.movie-card {
  flex: 0 0 auto;
  width: 160px;
  cursor: pointer;
  border-radius: 6px;
  overflow: hidden;
  transition: transform 0.2s, box-shadow 0.2s;
  position: relative;
  background: var(--card-bg);
}

.movie-card:hover {
  transform: scale(1.05);
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.6);
  z-index: 2;
}

.movie-card img {
  width: 100%;
  aspect-ratio: 2/3;
  object-fit: cover;
}

.movie-card-overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(to top, rgba(0,0,0,0.95) 0%, rgba(0,0,0,0.3) 50%, transparent 100%);
  display: flex;
  flex-direction: column;
  justify-content: flex-end;
  padding: 12px;
  opacity: 0;
  transition: opacity 0.2s;
}

.movie-card:hover .movie-card-overlay {
  opacity: 1;
}

.movie-card-title {
  font-size: 12px;
  font-weight: 700;
  color: var(--text-primary);
  line-height: 1.3;
  margin-bottom: 4px;
}

.movie-card-meta {
  font-size: 11px;
  color: var(--text-secondary);
  display: flex;
  align-items: center;
  gap: 6px;
}

.movie-card-rating {
  color: #f5c518;
  font-weight: 600;
}

.movie-card-img-placeholder {
  width: 100%;
  aspect-ratio: 2/3;
  background: #1a1a2e;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 11px;
  color: var(--text-secondary);
  text-align: center;
  padding: 8px;
}
```

- [ ] **Step 2: Create MovieCard.jsx**

Create `cinemap/src/components/MovieCard.jsx`:
```jsx
import './MovieCard.css'

const IMG_BASE = 'https://image.tmdb.org/t/p/w500'

export default function MovieCard({ movie, genres, onSelect }) {
  const year = movie.release_date?.slice(0, 4) ?? '—'
  const primaryGenre = genres[movie.genre_ids?.[0]] ?? ''

  return (
    <div className="movie-card" onClick={() => onSelect(movie)}>
      {movie.poster_path ? (
        <img
          src={`${IMG_BASE}${movie.poster_path}`}
          alt={movie.title}
          loading="lazy"
        />
      ) : (
        <div className="movie-card-img-placeholder">{movie.title}</div>
      )}
      <div className="movie-card-overlay">
        <div className="movie-card-title">{movie.title}</div>
        <div className="movie-card-meta">
          <span className="movie-card-rating">★ {movie.vote_average.toFixed(1)}</span>
          <span>{year}</span>
          {primaryGenre && <span>{primaryGenre}</span>}
        </div>
      </div>
    </div>
  )
}
```

- [ ] **Step 3: Commit**

```bash
cd /Users/waqiur/Personal/website_automation && git add cinemap/src/components/MovieCard.jsx cinemap/src/components/MovieCard.css && git commit -m "feat: add MovieCard component"
```

---

### Task 5: MovieModal component

**Files:**
- Create: `cinemap/src/components/MovieModal.jsx`
- Create: `cinemap/src/components/MovieModal.css`

**Interfaces:**
- Consumes: `movie` object (full shape from movies.js), `genres` map, `onClose` callback `() => void`
- Produces: `export default function MovieModal({ movie, genres, onClose })`

- [ ] **Step 1: Create MovieModal.css**

Create `cinemap/src/components/MovieModal.css`:
```css
.modal-backdrop {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.85);
  z-index: 200;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 24px;
}

.modal {
  background: #181818;
  border-radius: 12px;
  overflow: hidden;
  width: 100%;
  max-width: 860px;
  max-height: 90vh;
  overflow-y: auto;
  position: relative;
  box-shadow: 0 24px 80px rgba(0, 0, 0, 0.8);
}

.modal-backdrop-img {
  width: 100%;
  height: 360px;
  object-fit: cover;
  object-position: center top;
  display: block;
}

.modal-backdrop-placeholder {
  width: 100%;
  height: 360px;
  background: linear-gradient(135deg, #1a1a2e, #16213e);
}

.modal-close {
  position: absolute;
  top: 16px;
  right: 16px;
  width: 36px;
  height: 36px;
  border-radius: 50%;
  background: rgba(0, 0, 0, 0.7);
  color: white;
  font-size: 20px;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: background 0.2s;
  z-index: 10;
}

.modal-close:hover {
  background: rgba(0, 0, 0, 0.9);
}

.modal-body {
  display: flex;
  gap: 28px;
  padding: 28px;
}

.modal-poster {
  flex: 0 0 140px;
  border-radius: 8px;
  overflow: hidden;
  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.5);
  align-self: flex-start;
  margin-top: -80px;
  position: relative;
  z-index: 2;
}

.modal-poster img {
  width: 140px;
  aspect-ratio: 2/3;
  object-fit: cover;
}

.modal-info {
  flex: 1;
}

.modal-title {
  font-size: 26px;
  font-weight: 800;
  color: var(--text-primary);
  margin-bottom: 6px;
  line-height: 1.2;
}

.modal-tagline {
  font-size: 14px;
  color: var(--accent);
  font-style: italic;
  margin-bottom: 14px;
}

.modal-meta {
  display: flex;
  align-items: center;
  gap: 16px;
  flex-wrap: wrap;
  margin-bottom: 16px;
}

.modal-rating {
  color: #f5c518;
  font-weight: 700;
  font-size: 15px;
}

.modal-year,
.modal-runtime {
  color: var(--text-secondary);
  font-size: 13px;
}

.modal-genres {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  margin-bottom: 16px;
}

.modal-genre-tag {
  padding: 4px 12px;
  border-radius: 20px;
  border: 1px solid rgba(255, 255, 255, 0.2);
  font-size: 12px;
  color: var(--text-secondary);
}

.modal-overview {
  font-size: 14px;
  line-height: 1.7;
  color: #cccccc;
}

.modal-play-btn {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  background: var(--accent);
  color: white;
  font-size: 14px;
  font-weight: 700;
  padding: 10px 24px;
  border-radius: 6px;
  margin-top: 20px;
  transition: background 0.2s;
}

.modal-play-btn:hover {
  background: #c0000f;
}
```

- [ ] **Step 2: Create MovieModal.jsx**

Create `cinemap/src/components/MovieModal.jsx`:
```jsx
import { useEffect } from 'react'
import './MovieModal.css'

const POSTER_BASE = 'https://image.tmdb.org/t/p/w500'
const BACKDROP_BASE = 'https://image.tmdb.org/t/p/original'

export default function MovieModal({ movie, genres, onClose }) {
  useEffect(() => {
    const onKey = (e) => { if (e.key === 'Escape') onClose() }
    window.addEventListener('keydown', onKey)
    document.body.style.overflow = 'hidden'
    return () => {
      window.removeEventListener('keydown', onKey)
      document.body.style.overflow = ''
    }
  }, [onClose])

  const year = movie.release_date?.slice(0, 4) ?? '—'
  const runtime = movie.runtime ? `${Math.floor(movie.runtime / 60)}h ${movie.runtime % 60}m` : null
  const movieGenres = (movie.genre_ids ?? []).map(id => genres[id]).filter(Boolean)

  return (
    <div className="modal-backdrop" onClick={onClose}>
      <div className="modal" onClick={(e) => e.stopPropagation()}>
        <button className="modal-close" onClick={onClose} aria-label="Close">✕</button>

        {movie.backdrop_path ? (
          <img
            className="modal-backdrop-img"
            src={`${BACKDROP_BASE}${movie.backdrop_path}`}
            alt=""
          />
        ) : (
          <div className="modal-backdrop-placeholder" />
        )}

        <div className="modal-body">
          <div className="modal-poster">
            {movie.poster_path && (
              <img src={`${POSTER_BASE}${movie.poster_path}`} alt={movie.title} />
            )}
          </div>

          <div className="modal-info">
            <h2 className="modal-title">{movie.title}</h2>
            {movie.tagline && <p className="modal-tagline">"{movie.tagline}"</p>}

            <div className="modal-meta">
              <span className="modal-rating">★ {movie.vote_average.toFixed(1)}</span>
              <span className="modal-year">{year}</span>
              {runtime && <span className="modal-runtime">{runtime}</span>}
            </div>

            {movieGenres.length > 0 && (
              <div className="modal-genres">
                {movieGenres.map(g => (
                  <span key={g} className="modal-genre-tag">{g}</span>
                ))}
              </div>
            )}

            <p className="modal-overview">{movie.overview}</p>

            <button className="modal-play-btn">▶ Play</button>
          </div>
        </div>
      </div>
    </div>
  )
}
```

- [ ] **Step 3: Commit**

```bash
cd /Users/waqiur/Personal/website_automation && git add cinemap/src/components/MovieModal.jsx cinemap/src/components/MovieModal.css && git commit -m "feat: add MovieModal component"
```

---

### Task 6: MovieRow component

**Files:**
- Create: `cinemap/src/components/MovieRow.jsx`
- Create: `cinemap/src/components/MovieRow.css`

**Interfaces:**
- Consumes: `title` string, `movies` array, `genres` map, `onSelect` callback `(movie) => void`
- Produces: `export default function MovieRow({ title, movies, genres, onSelect })`

- [ ] **Step 1: Create MovieRow.css**

Create `cinemap/src/components/MovieRow.css`:
```css
.movie-row {
  margin-bottom: 40px;
}

.movie-row-header {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 0 48px;
  margin-bottom: 16px;
}

.movie-row-accent {
  width: 4px;
  height: 22px;
  background: var(--accent);
  border-radius: 2px;
  flex-shrink: 0;
}

.movie-row-title {
  font-size: 18px;
  font-weight: 700;
  color: var(--text-primary);
}

.movie-row-track-wrapper {
  position: relative;
}

.movie-row-track {
  display: flex;
  gap: 12px;
  overflow-x: auto;
  scroll-behavior: smooth;
  padding: 8px 48px 16px;
  scrollbar-width: none;
}

.movie-row-track::-webkit-scrollbar {
  display: none;
}

.movie-row-arrow {
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  width: 40px;
  height: 40px;
  border-radius: 50%;
  background: rgba(0, 0, 0, 0.7);
  color: white;
  font-size: 18px;
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 5;
  transition: background 0.2s, opacity 0.2s;
  border: 1px solid rgba(255,255,255,0.15);
}

.movie-row-arrow:hover {
  background: rgba(0, 0, 0, 0.95);
}

.movie-row-arrow.left {
  left: 6px;
}

.movie-row-arrow.right {
  right: 6px;
}
```

- [ ] **Step 2: Create MovieRow.jsx**

Create `cinemap/src/components/MovieRow.jsx`:
```jsx
import { useRef } from 'react'
import MovieCard from './MovieCard.jsx'
import './MovieRow.css'

const SCROLL_AMOUNT = 600

export default function MovieRow({ title, movies, genres, onSelect }) {
  const trackRef = useRef(null)

  const scroll = (dir) => {
    if (!trackRef.current) return
    trackRef.current.scrollBy({ left: dir * SCROLL_AMOUNT, behavior: 'smooth' })
  }

  if (!movies || movies.length === 0) return null

  return (
    <section className="movie-row">
      <div className="movie-row-header">
        <div className="movie-row-accent" />
        <h2 className="movie-row-title">{title}</h2>
      </div>
      <div className="movie-row-track-wrapper">
        <button className="movie-row-arrow left" onClick={() => scroll(-1)} aria-label="Scroll left">‹</button>
        <div className="movie-row-track" ref={trackRef}>
          {movies.map(movie => (
            <MovieCard key={movie.id} movie={movie} genres={genres} onSelect={onSelect} />
          ))}
        </div>
        <button className="movie-row-arrow right" onClick={() => scroll(1)} aria-label="Scroll right">›</button>
      </div>
    </section>
  )
}
```

- [ ] **Step 3: Commit**

```bash
cd /Users/waqiur/Personal/website_automation && git add cinemap/src/components/MovieRow.jsx cinemap/src/components/MovieRow.css && git commit -m "feat: add MovieRow carousel component"
```

---

### Task 7: HeroBanner component

**Files:**
- Create: `cinemap/src/components/HeroBanner.jsx`
- Create: `cinemap/src/components/HeroBanner.css`

**Interfaces:**
- Consumes: `movies` array (full movie objects for featured films), `genres` map, `onSelect` callback `(movie) => void`
- Produces: `export default function HeroBanner({ movies, genres, onSelect })`

- [ ] **Step 1: Create HeroBanner.css**

Create `cinemap/src/components/HeroBanner.css`:
```css
.hero {
  position: relative;
  height: 560px;
  overflow: hidden;
  margin-bottom: 40px;
}

.hero-backdrop {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center 20%;
  transition: opacity 0.8s;
}

.hero-gradient {
  position: absolute;
  inset: 0;
  background: linear-gradient(
    to right,
    rgba(13, 13, 13, 0.95) 0%,
    rgba(13, 13, 13, 0.6) 50%,
    transparent 100%
  ),
  linear-gradient(
    to top,
    rgba(13, 13, 13, 1) 0%,
    transparent 40%
  );
}

.hero-content {
  position: absolute;
  bottom: 80px;
  left: 48px;
  max-width: 500px;
}

.hero-genres {
  display: flex;
  gap: 8px;
  margin-bottom: 12px;
  flex-wrap: wrap;
}

.hero-genre-pill {
  font-size: 11px;
  font-weight: 600;
  color: var(--accent);
  text-transform: uppercase;
  letter-spacing: 1px;
}

.hero-genre-pill + .hero-genre-pill::before {
  content: '·';
  margin-right: 8px;
  color: var(--text-secondary);
}

.hero-title {
  font-size: 48px;
  font-weight: 900;
  line-height: 1;
  letter-spacing: -1.5px;
  text-transform: uppercase;
  color: var(--text-primary);
  margin-bottom: 12px;
  text-shadow: 0 4px 20px rgba(0,0,0,0.5);
}

.hero-meta {
  display: flex;
  align-items: center;
  gap: 16px;
  margin-bottom: 16px;
}

.hero-rating {
  color: #f5c518;
  font-weight: 700;
  font-size: 14px;
}

.hero-year {
  color: var(--text-secondary);
  font-size: 13px;
}

.hero-overview {
  font-size: 14px;
  line-height: 1.7;
  color: #cccccc;
  margin-bottom: 24px;
  display: -webkit-box;
  -webkit-line-clamp: 3;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.hero-ctas {
  display: flex;
  gap: 12px;
}

.hero-btn-play {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  background: var(--accent);
  color: white;
  font-size: 14px;
  font-weight: 700;
  padding: 12px 28px;
  border-radius: 6px;
  transition: background 0.2s;
}

.hero-btn-play:hover {
  background: #c0000f;
}

.hero-btn-info {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  background: rgba(255, 255, 255, 0.15);
  backdrop-filter: blur(4px);
  color: white;
  font-size: 14px;
  font-weight: 600;
  padding: 12px 28px;
  border-radius: 6px;
  border: 1px solid rgba(255,255,255,0.2);
  transition: background 0.2s;
}

.hero-btn-info:hover {
  background: rgba(255, 255, 255, 0.25);
}

.hero-dots {
  position: absolute;
  bottom: 32px;
  left: 48px;
  display: flex;
  gap: 8px;
}

.hero-dot {
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: rgba(255,255,255,0.3);
  transition: background 0.3s, transform 0.3s;
}

.hero-dot.active {
  background: var(--accent);
  transform: scale(1.4);
}
```

- [ ] **Step 2: Create HeroBanner.jsx**

Create `cinemap/src/components/HeroBanner.jsx`:
```jsx
import { useState, useEffect, useCallback } from 'react'
import './HeroBanner.css'

const BACKDROP_BASE = 'https://image.tmdb.org/t/p/original'
const ROTATE_MS = 6000

export default function HeroBanner({ movies, genres, onSelect }) {
  const [index, setIndex] = useState(0)

  const next = useCallback(() => {
    setIndex(i => (i + 1) % movies.length)
  }, [movies.length])

  useEffect(() => {
    const timer = setInterval(next, ROTATE_MS)
    return () => clearInterval(timer)
  }, [next])

  if (!movies || movies.length === 0) return null

  const movie = movies[index]
  const year = movie.release_date?.slice(0, 4) ?? '—'
  const movieGenres = (movie.genre_ids ?? []).slice(0, 3).map(id => genres[id]).filter(Boolean)

  return (
    <div className="hero">
      {movie.backdrop_path && (
        <img
          key={movie.id}
          className="hero-backdrop"
          src={`${BACKDROP_BASE}${movie.backdrop_path}`}
          alt=""
        />
      )}
      <div className="hero-gradient" />

      <div className="hero-content">
        {movieGenres.length > 0 && (
          <div className="hero-genres">
            {movieGenres.map(g => (
              <span key={g} className="hero-genre-pill">{g}</span>
            ))}
          </div>
        )}

        <h1 className="hero-title">{movie.title}</h1>

        <div className="hero-meta">
          <span className="hero-rating">★ {movie.vote_average.toFixed(1)}</span>
          <span className="hero-year">{year}</span>
          {movie.runtime && (
            <span className="hero-year">{Math.floor(movie.runtime / 60)}h {movie.runtime % 60}m</span>
          )}
        </div>

        <p className="hero-overview">{movie.overview}</p>

        <div className="hero-ctas">
          <button className="hero-btn-play">▶ Play</button>
          <button className="hero-btn-info" onClick={() => onSelect(movie)}>ℹ More Info</button>
        </div>
      </div>

      <div className="hero-dots">
        {movies.map((_, i) => (
          <button
            key={i}
            className={`hero-dot${i === index ? ' active' : ''}`}
            onClick={() => setIndex(i)}
            aria-label={`Show movie ${i + 1}`}
          />
        ))}
      </div>
    </div>
  )
}
```

- [ ] **Step 3: Commit**

```bash
cd /Users/waqiur/Personal/website_automation && git add cinemap/src/components/HeroBanner.jsx cinemap/src/components/HeroBanner.css && git commit -m "feat: add HeroBanner with auto-rotation"
```

---

### Task 8: Home page — assemble everything

**Files:**
- Create: `cinemap/src/pages/Home.jsx`
- Modify: `cinemap/src/App.jsx`

**Interfaces:**
- Consumes: all components from Tasks 3–7, `movies` + `genres` + `FEATURED_IDS` from `src/data/movies.js`
- Produces: complete working single-page app

- [ ] **Step 1: Create Home.jsx**

Create `cinemap/src/pages/Home.jsx`:
```jsx
import { useState } from 'react'
import { movies, genres, FEATURED_IDS } from '../data/movies.js'
import HeroBanner from '../components/HeroBanner.jsx'
import MovieRow from '../components/MovieRow.jsx'
import MovieModal from '../components/MovieModal.jsx'

const featuredMovies = FEATURED_IDS.map(id => movies.find(m => m.id === id)).filter(Boolean)

const rows = [
  { title: 'Trending Now', movies: movies.slice(0, 8) },
  { title: 'Top Rated', movies: [...movies].sort((a, b) => b.vote_average - a.vote_average).slice(0, 10) },
  { title: 'Action', movies: movies.filter(m => m.genre_ids.includes(28)) },
  { title: 'Drama', movies: movies.filter(m => m.genre_ids.includes(18)) },
  { title: 'Sci-Fi', movies: movies.filter(m => m.genre_ids.includes(878)) },
  { title: 'Crime & Thriller', movies: movies.filter(m => m.genre_ids.includes(80) || m.genre_ids.includes(53)) },
]

export default function Home() {
  const [selected, setSelected] = useState(null)

  return (
    <main>
      <HeroBanner movies={featuredMovies} genres={genres} onSelect={setSelected} />

      {rows.map(row => (
        row.movies.length > 0 && (
          <MovieRow
            key={row.title}
            title={row.title}
            movies={row.movies}
            genres={genres}
            onSelect={setSelected}
          />
        )
      ))}

      {selected && (
        <MovieModal movie={selected} genres={genres} onClose={() => setSelected(null)} />
      )}
    </main>
  )
}
```

- [ ] **Step 2: Update App.jsx**

Replace `cinemap/src/App.jsx`:
```jsx
import Navbar from './components/Navbar.jsx'
import Home from './pages/Home.jsx'

export default function App() {
  return (
    <>
      <Navbar />
      <Home />
    </>
  )
}
```

- [ ] **Step 3: Verify the full app in the browser**

Open `http://localhost:5173` and confirm:
- Dark navbar with Cinemap logo is fixed at top
- Hero banner shows a movie with backdrop image, title, rating, genres, Play and More Info buttons
- Hero auto-rotates every 6 seconds; dots at bottom reflect current index
- At least 5 movie rows visible below hero, each with horizontal scroll
- Movie cards show poster images with hover overlay (title + rating)
- Clicking a card opens the modal with full movie details
- Clicking ✕ or outside modal closes it
- Pressing Escape closes modal

- [ ] **Step 4: Commit**

```bash
cd /Users/waqiur/Personal/website_automation && git add cinemap/src/pages/Home.jsx cinemap/src/App.jsx && git commit -m "feat: assemble Home page — Cinemap complete"
```

---

### Task 9: Footer + polish pass

**Files:**
- Create: `cinemap/src/components/Footer.jsx`
- Create: `cinemap/src/components/Footer.css`
- Modify: `cinemap/src/App.jsx`

**Interfaces:**
- Produces: `export default function Footer()` — static footer with disclaimer

- [ ] **Step 1: Create Footer.css**

Create `cinemap/src/components/Footer.css`:
```css
.footer {
  margin-top: 60px;
  padding: 40px 48px;
  border-top: 1px solid rgba(255, 255, 255, 0.08);
}

.footer-logo {
  font-size: 20px;
  font-weight: 800;
  color: var(--text-primary);
  margin-bottom: 12px;
}

.footer-logo span {
  color: var(--accent);
}

.footer-disclaimer {
  font-size: 12px;
  color: var(--text-secondary);
  max-width: 500px;
  line-height: 1.6;
}
```

- [ ] **Step 2: Create Footer.jsx**

Create `cinemap/src/components/Footer.jsx`:
```jsx
import './Footer.css'

export default function Footer() {
  return (
    <footer className="footer">
      <div className="footer-logo">
        <span>C</span>inemap
      </div>
      <p className="footer-disclaimer">
        This site does not store any files. All content references are for demonstration purposes only.
        Movie data shaped after TMDB API format. Images © TMDB.
      </p>
    </footer>
  )
}
```

- [ ] **Step 3: Add Footer to App.jsx**

Replace `cinemap/src/App.jsx`:
```jsx
import Navbar from './components/Navbar.jsx'
import Home from './pages/Home.jsx'
import Footer from './components/Footer.jsx'

export default function App() {
  return (
    <>
      <Navbar />
      <Home />
      <Footer />
    </>
  )
}
```

- [ ] **Step 4: Final visual check**

Open `http://localhost:5173`. Scroll to the bottom — confirm footer appears with "Cinemap" logo and disclaimer text.

- [ ] **Step 5: Final commit**

```bash
cd /Users/waqiur/Personal/website_automation && git add cinemap/src/components/Footer.jsx cinemap/src/components/Footer.css cinemap/src/App.jsx && git commit -m "feat: add footer, cinemap v1 complete"
```
